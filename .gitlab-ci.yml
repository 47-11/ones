image: maven:3.8-openjdk-16

stages:
  - init
  - validate
  - build
  - test

init:
  stage: init
  script:
    - mvn clean

validate:
  stage: validate
  script:
    - mvn validate

build:
  stage: build
  script:
    - mvn compile

test:
  stage: test
  script:
    - mvn test verify
